# YourApp

完整 WinForms 成品示例：
- 暗色 UI
- Win11 Mica
- WebView2（MSI 可另行加入）
- Squirrel 自动更新（有更新才提示）
- 设置页：开启 / 关闭自动更新

## 使用
1. 用 Visual Studio 打开 `YourApp.csproj`
2. 还原 NuGet
3. 直接运行

## 更新
修改 `UpdateHelper.cs` 中的 UpdateUrl 为你的发布地址。